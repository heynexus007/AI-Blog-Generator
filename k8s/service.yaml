apiVersion: v1
kind: Service
metadata:
  name: ai-blog-generator-service
  namespace: ai-blog-generator
spec:
  selector:
    app: ai-blog-generator
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
  type: ClusterIP